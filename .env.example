# Copy this to .env.local
# Point this to Daniel's backend once it's running
VITE_API_URL=http://localhost:4455/api/v1

# Set to true to use mock data instead of real API (remove when backend ready)
VITE_USE_MOCKS=false
